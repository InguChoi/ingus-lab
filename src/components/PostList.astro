---
import type { CollectionEntry } from "astro:content";
import FormattedDate from "./FormattedDate.astro";

interface Props {
	posts: CollectionEntry<"blog">[];
}

const { posts } = Astro.props;
---

{
	posts.length > 0 ? (
		<ul class="post-list">
			{posts.map((post) => (
				<li>
					<a href={`/blog/${post.id}/`}>
						{post.data.heroImage && (
							<img width={720} height={360} src={post.data.heroImage} alt="" />
						)}
						<div>
							<p class="meta">
								<FormattedDate date={post.data.pubDate} />
							</p>
							<h2>
								<span data-lang="ko">{post.data.titleKo ?? post.data.title}</span>
								<span data-lang="en">{post.data.titleEn ?? post.data.title}</span>
							</h2>
							<p>
								<span data-lang="ko">{post.data.descriptionKo ?? post.data.description}</span>
								<span data-lang="en">{post.data.descriptionEn ?? post.data.description}</span>
							</p>
							{post.data.tags && post.data.tags.length > 0 && (
								<ul class="tags" aria-label="Keywords">
									{post.data.tags.map((tag) => (
										<li>#{tag}</li>
									))}
								</ul>
							)}
						</div>
					</a>
				</li>
			))}
		</ul>
	) : (
		<p class="empty">아직 등록된 글이 없습니다.</p>
	)
}

<style>
	.post-list {
		display: grid;
		gap: 1.5rem;
		list-style: none;
		margin: 0;
		padding: 0;
	}
	.post-list li {
		border-top: 1px solid rgb(var(--gray-light));
		padding-top: 1.5rem;
	}
	.post-list a {
		display: grid;
		grid-template-columns: minmax(160px, 240px) 1fr;
		gap: 1.25rem;
		color: rgb(var(--gray-dark));
		text-decoration: none;
	}
	.post-list img {
		aspect-ratio: 2 / 1;
		height: 100%;
		object-fit: cover;
		width: 100%;
	}
	.post-list h2 {
		font-size: 1.25em;
		margin-bottom: 0.25rem;
	}
	.post-list p {
		margin: 0;
	}
	.tags {
		display: flex;
		flex-wrap: wrap;
		gap: 0.35rem;
		list-style: none;
		margin: 0.75rem 0 0;
		padding: 0;
	}
	.tags li {
		border: 1px solid rgb(var(--gray-light));
		border-radius: 999px;
		color: rgb(var(--gray));
		font-size: 0.76rem;
		font-weight: 700;
		line-height: 1;
		padding: 0.38rem 0.55rem;
	}
	.post-list .meta {
		color: rgb(var(--gray));
		font-size: 0.85em;
		margin-bottom: 0.25rem;
	}
	.post-list a:hover h2 {
		color: var(--accent);
	}
	.empty {
		border-top: 1px solid rgb(var(--gray-light));
		color: rgb(var(--gray));
		margin: 0;
		padding-top: 1.5rem;
	}
	@media (max-width: 720px) {
		.post-list a {
			grid-template-columns: 1fr;
		}
	}
</style>
