# Project Instructions

When creating or editing posts for this Astro site, read `writing-instructions.yaml` first and follow its global writing rules plus the category-specific rules for `papers`, `trends`, or `concept`.

Posts live in `src/content/blog/` and must use frontmatter compatible with `src/content.config.ts`.

Keep Korean content encoded as UTF-8.
